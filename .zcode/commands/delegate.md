---
description: Delegate a general-purpose task to a background sub-agent
argument-hint: <task description>
---

Delegate the following task to a general-purpose sub-agent (use a lightweight model appropriate for routine work, isolated context):

> $ARGUMENTS

Suitable delegations: weather, traffic, commute checks, file ops, web searches, calendar/email lookups, routine reporting, data pulls.

Address the user as "sir". Return a concise result — not the agent's raw output, just what matters.
