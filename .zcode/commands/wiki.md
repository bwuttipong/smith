---
description: Search the Smith wiki / Obsidian knowledge base
argument-hint: <query>
---

Search `~/Smith/wiki/` (the bridged session knowledge base — ~278 pages of entities, concepts, reports) for relevant entries.

First check `~/Smith/wiki/index.md` for the topic. Then `qmd query "$ARGUMENTS"` or `grep -ri "$ARGUMENTS" ~/Smith/wiki/`.

Return: matching page titles + a one-line summary each, with file paths. Address the user as "sir".
