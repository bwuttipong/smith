
## 07:29:57 +07 — OmniRouter custom provider / OpenCode free model sweep
- Checked OmniRoute proxy at `http://127.0.0.1:20128/v1` for custom-provider models usable by OpenCode/OpenAI-compatible clients.
- `/v1/models` exposed 220 models. Generic free routes that passed chat smoke test: `auto/coding:free`, `kc/openrouter/free`, `kilocode/openrouter/free`.
- OpenCode namespace models advertised: `opencode/big-pickle`, `opencode/deepseek-v4-flash-free`, `opencode/mimo-v2.5-free`, `opencode/ling-3.0-flash-free`, `opencode/nemotron-3-ultra-free`, `opencode/north-mini-code-free`, `opencode/laguna-s-2.1-free`, `opencode/longcat-2.0-free`; all returned HTTP 403 `insufficient_quota` during smoke tests.
- Artifacts: `/Users/Jeff/Smith/memory/artifacts/2026-08-06-0728-omnirouter-custom-provider-free-models.json`, `/Users/Jeff/Smith/memory/artifacts/2026-08-06-0729-omnirouter-opencode-namespace-smoke.json`.

## 08:27:30 +07 — LINE handoff for OmniRouter/OpenCode model check
- Jeff said he will continue chatting with Smith on LINE.
- Wrote handoff artifact: `/Users/Jeff/Smith/memory/artifacts/2026-08-06-line-handoff-omnirouter.md`.
- Key handoff: OmniRouter custom provider endpoint `http://127.0.0.1:20128/v1`; usable free routes are `auto/coding:free`, `kc/openrouter/free`, `kilocode/openrouter/free`; advertised `opencode/*-free` routes currently fail with HTTP 403 `insufficient_quota`.

## 08:38:43 +07 — OmniRouter free model session switch check
- Jeff asked to temporarily use a free OmniRouter model for this LINE session with tool-calling and decent reasoning.
- Smoke-tested OmniRouter `/v1/chat/completions` with an OpenAI tools schema. Passing tool-call candidates: `auto/best-free` (6.46s), `kc/openrouter/free` (1.31s), `kilocode/openrouter/free` (0.02s cached), `auto/smart`, `auto/coding:cheap`.
- `auto/coding:free` failed with HTTP 403 `insufficient_quota` via opencode/big-pickle.
- Recommended session-only command: `/model auto/best-free --provider omnirouter --session`; faster fallback: `/model kc/openrouter/free --provider omnirouter --session`.
- Test script saved at `/Users/Jeff/Smith/scripts/omni_free_tool_smoke.py`.

## 2026-08-06 evening — Morning brief Todoist fix (round 2)
- Root cause: `todoist tasks` with no project filter only returns 6 tasks (default view); Work 🎯 project query returns 14 incl. real tasks (Box deploy, StoreInk, ERP MRP).
- Fix: morningbrief.py now fetches ALL active tasks via Todoist REST API (token from env or ~/Smith/.env), falls back to per-project CLI queries; filters onboarding cruft (quiz, getting-started, workday routines, recurring inbox items) via cleaned-content markers; ranks due-today first.
- Verified live: brief now shows [Box] Deploy 8 Aug, [StoreInk] Defective Write-Off 8 Aug, [ERP] MRP Infor Food Packaging.
- Committed 6866ae6e, pushed to master.

## 2026-08-06 evening — Session handoff (LINE → Telegram)
- Fixed morning brief Todoist visibility (root cause: `todoist tasks` CLI default view returns only 6 tasks; REST API fetch of all active tasks + onboarding-cruft filter now surfaces real work tasks). Commits: 3d32e9e3, 6866ae6e (pushed).
- Discovered Hermes profiles live at ~/.hermes/profiles/ (smith active; coder/researcher/writer also exist).
- Wired all subagent personas as symlinked profiles: ba, frontend-dev, backend-dev, qa, adversary, orchestrator + Beaker/Bunsen/Cookie/Elmo/Fozzie/Kermit/Samantha/janitor → visible in Hermes Desktop profile switcher (18 profiles total).
- Claude Code on Windows (Wuttipong.t): claude not on PATH; set npm prefix to .npm-global, install global package, use env vars ANTHROPIC_BASE_URL=https://openrouter.ai/api/v1 + ANTHROPIC_API_KEY + ANTHROPIC_MODEL=qwen/qwen-2.5-coder-32b-instruct:free for free tier; new PowerShell window required after setx/setenv.
