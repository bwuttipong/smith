# 2026-07-01

## QA Tester Skill Location Updates
- Moved the `qa_tester` skill folder from `skills/qa_tester` to `.agents/skills/qa_tester` for workspace customization root discovery.
- Moved the `qa_tester` skill back to `skills/qa_tester` per user request.

## Moved CirculatingBox Publish Skill
- Moved `circulatingbox-publish` from the global customizations root (`C:\Users\Wuttipong.t\.gemini\config\skills\circulatingbox_publish`) to `skills/circulatingbox_publish` in the Smith workspace.
- Updated path references to `publisher.py` inside `SKILL.md` to point to the new workspace-based location.

