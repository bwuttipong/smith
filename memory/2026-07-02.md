# 2026-07-02

## Morning
- Disabled `memory-midnight-maintenance` cron job (was under samantha's agent, not smith's — lesson: always scan all agents)
- Job was failing due to `opencode-go/deepseek-v4-flash` rejected by models allowlist
- OpenCode tokens depleted — Jeff bought Xiaomi MiMo as replacement
- Model: `xiaomi-token-plan/mimo-v2.5-pro` (current default)

## MCP Memory Bridge (Built Today)
- **Purpose**: Shared memory server for OpenClaw + Hermes Agent interop
- **Location**: `~/Smith/tools/mcp-memory-bridge/`
- **Files**: `server.py` (MCP server), `store.py` (SQLite FTS5 backend), `config.json`, `README.md`, `requirements.txt`
- **What it does**: Indexes `~/Smith/memory/` and `~/Smith/wiki/` (552 files, 2.2MB) into SQLite with FTS5 full-text search
- **MCP Tools exposed**: `memory_bridge_scan`, `memory_bridge_search`, `memory_bridge_read`, `memory_bridge_write`, `memory_bridge_list`, `memory_bridge_get`, `memory_bridge_stats`
- **Wired into OpenClaw**: `openclaw mcp add memory-bridge --command python3 --arg /Users/Jeff/Smith/tools/mcp-memory-bridge/server.py`
- **Status**: ✅ built, wired, gateway restarted

### Hermes Side (TODO)
- Install Hermes Agent: `curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash`
- Add MCP server: `hermes mcp add memory-bridge --command "python3 /Users/Jeff/Smith/tools/mcp-memory-bridge/server.py"`
- Choose free model provider (GLM, OpenRouter free tier, or Ollama local)

## Interop Audit (09:14 GMT+7)
- **Result: 10/10 pass. Fully compatible.**
- Both agents confirmed: same DB stats, same search results, cross-write works, file reads match, sessions isolated, tools isolated, profiles isolated.
- Note from Hermes: MCP tools load at session start — `/reset` in Hermes makes memory_bridge_* tools available as first-class tools.
- Audit files: `~/Smith/tools/mcp-memory-bridge/AUDIT.md`, `AUDIT-SMITH-SIDE.md`

## Kilo Code Setup (16:21 GMT+7)
- Company approved VS Code for Jeff's work machine
- Created Kilo Code MCP config templates (macOS + Windows)
- Updated README with all 4 surface setup instructions
- Committed & pushed to github.com/bwuttipong/smith
- Work machine needs: git pull, Python, VS Code + Kilo Code extension, OpenRouter API key
- Windows config assumes `C:\Users\Wuttipong.t\Smith\` — may need path adjustment

## Antigravity Research + Config (11:03 GMT+7)
- Researched Google Antigravity 2.0 — agentic coding IDE + CLI, launched at Google I/O May 2026
- Replaces deprecated Gemini CLI
- Supports MCP natively → our MCP Memory Bridge works with it
- Config written to `~/.gemini/config/mcp_config.json` with memory-bridge entry
- Both paths symlinked (`~/.gemini/config/` ↔ `~/.gemini/antigravity/`)
- Pricing: Free/$20/$100/$200 tiers, free tier rate-limited (quotas cut 4x since Dec 2025)
- Added Antigravity section to TOOLS.md with future-session reminder
- **Three-agent interop now configured:** OpenClaw + Hermes + Antigravity → shared MCP Memory Bridge

## Exec Approvals (10:15 GMT+7)
- Set `tools.exec.ask: "always"` + synced host approvals file via `openclaw exec-policy set --ask always --security allowlist`
- Telegram IS the approval surface — no companion app needed
- Every exec call shows `/approve <id> allow-once` or `/approve <id> deny`
- Matches Hermes's `approvals.mode: manual` for shell commands
- Note: OpenClaw approvals cover exec only (not file reads/writes like Hermes)

## Milestone: Seamless Memory Sync (10:53 GMT+7)
- Jeff confirmed memory sync feels seamless between OpenClaw and Hermes
- Both agents finding same answers from shared MCP Memory Bridge
- "What a surprise!" — the interop actually works in practice, not just in audit tests

## Notes
- Brave Search API rate limited (HTTP 429) — use Prismfy backup when needed

## Kilo Code Workspace Config (21:30 GMT+7)
- Created `kilo.json` at workspace root with `.kilo/command` and `.kilo/agent` dirs
- Built `.kilo/command/` with 7 slash commands: `soul`, `memory`, `morning`, `evening`, `wiki`, `weekly`, `search` (all wired to skills)
- Built `.kilo/agent/smith.md` — full SOUL.md system prompt + all tools enabled
- Cleaned accidental `node_modules` from `.kilo/` (npm install leak)
- Global MCP config for memory-bridge already written to `~/Library/Application Support/Code/User/globalStorage/kilocode.kilo-code/kilo.jsonc`
- Repo committed & pushed: github.com/bwuttipong/smith — portable to work machine

## Handoff for Tomorrow at Work (Windows 11)
- Clone repo: `git clone https://github.com/bwuttipong/smith.git`
- Open in VS Code → Kilo loads `.kilo/` automatically
- Only change needed: update MCP server path in Kilo settings to Windows path `C:\Users\Wuttipong.t\Smith\tools\mcp-memory-bridge\server.py`
- Slash commands ready: `/soul` reloads identity, `/search` hits shared memory-bridge, `/memory` logs to today's file
- Smith agent persona loaded with full SOUL.md context
- Memory bridge index: 553 files, 5 structured entries — cross-agent search works
