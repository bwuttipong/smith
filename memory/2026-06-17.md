# 2026-06-17

- session startup: smith online.
- checked outsourceef9 project: target framework is net9.0-windows, ef core 9.0.0, closedxml 0.105.0.
- checked software version: defaults to 1.0.0 (not explicitly defined in .csproj or custom AssemblyInfo).
- confirmed no git repository is initialized in the workspace directory.
- initialized git repository, added standard .gitignore, removed tracked obj files, and made initial commit.
- discovered database connection configuration is loaded from network share: \\192.168.10.2\ShareCenter\Program\Outsource\config.json.
- refactored DbConfigHelper to load config path from environment variable OUTSOURCE_CONFIG_PATH, falling back to new path \\192.168.95.200\TPKShare\IT\Outsource\config.json.
- verified code compiles successfully with dotnet build and committed all files to Git.
- set OUTSOURCE_CONFIG_PATH environment variable to \\192.168.95.200\TPKShare\IT\Outsource\config.json on the user's laptop.
- initialized VS Code configurations (.vscode/tasks.json, .vscode/launch.json) for building and debugging in light IDE.
- updated ClickOnce publish profile, compiled release build using MSBuild, and successfully deployed version 1.0.0.64 to network share: \\192.168.95.200\TPKShare\IT\Outsource\OutsourceEF9\.
- created publish.ps1 script to automate MSBuild ClickOnce compilation and deployment to target network share, committed to Git.
- created IT environment setup and ClickOnce deployment manual (with Thai translation).
- copied English and Thai IT setup manuals to user's Desktop.
- created and committed agent.md in the project root to document environment variables, build tasks, and database contexts for future agent sessions.
- documented Sticky Notes (Windows/OneNote) automation commands and script in TOOLS.md.
- created and initialized global `sticky-notes` skill inside Smith directory (SKILL.md + scripts/sticky.ps1).
- session wrap-up: successfully completed outsource config path decoupling, git repo setup, clickonce profile update, automated deployment script, and IT setup documentation (copied to Desktop). smith signing off for the day.
- **LINE handler fix**: discovered and fixed the hexdigest bug in `skills/line/handler.py`. LINE sends `X-Line-Signature` as base64-encoded, but handler was comparing it against hex-encoded digest (`mac.hexdigest()`). Every webhook was getting 403'd. Fix: `base64.b64encode(mac.digest()).decode()`. Verified working end-to-end.
- **Cloudflare tunnel setup**: created `line-bot` tunnel for stable webhook URL `line.bestwuttipong.dev` → `localhost:8081`. Routed DNS with `--overwrite-dns`. Wrote `~/.cloudflared/config.yml`. Both handler and cloudflared need manual restart after reboot (not yet set up as service).
- handler now runs on port 8081 (old bugged instance stuck on 8080 PID 5824, couldn't kill). ngrok killed and replaced with cloudflared.
