# Agents

Sanitized source-control snapshot of `~/Agents`.

Committed here: persona definitions, routing notes, heartbeats, tool notes, and lightweight JSON workspace state.

Intentionally excluded: runtime `agent/` homes, nested `.git/`, `.openclaw/`, caches, SQLite databases, OAuth/auth files, logs, sessions, and macOS metadata. Those belong on Jeff's machine, not GitHub — even a private repo does not deserve loose credentials.
