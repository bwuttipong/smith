# Session Evolution — 2026-06-04

## Changes Made

### 1. Gateway — Tailscale Serve Enabled
- **What:** `gateway.tailscale.mode` → `serve`
- **Why:** Windows machine (fx-programer-nb1, tailscale IP 100.104.52.34) needs to connect as a node host from office. Gateway was loopback-only.
- **Result:** Gateway accessible at `https://wuttipong-1.tail6c0b11.ts.net:443` → local `:18789`
- **Pending:** Windows node install & device pairing approval

### 2. Daily Vocab Cron — Model Swap
- **Job:** `daily-vocab-drip` (9, 11, 13, 15, 17 BKK)
- **Old model:** `openrouter/nvidia/nemotron-3-super-120b-a12b:free` — 5 consecutive context overflows
- **New model:** `openrouter/owl-alpha` — free, 1M context window ✅
- **Timeout:** bumped 120s → 300s (owl-alpha can be slow on free tier)
- **Cost:** old was free, new is also free
- **Tested:** manual run at ~10:48 — returned word "approximately" to LINE in 78s
- **11:00 run:** timed out at 120s (before timeout bump). 13:00 should work with 300s.

### 3. openclaw-web-search Plugin — Fixed (Disabled)
- **Issue:** Plugin only had TypeScript source in extensions dir, no compiled JS
- **Fix:** Copied `dist/` from npm package + correct `openclaw.plugin.json` to extensions dir
- **Reality:** Plugin requires Zhipu AI API key — we don't have one. Disabled to stop warnings.
- **Backup:** Brave search (primary) + Prismfy (backup) already cover search needs.

### 4. last30days-openclaw Skill — State Audit
- **Location:** `~/.agents/skills/last30days-openclaw/`
- **Working:** Brave search, YouTube, HackerNews, Polymarket
- **Partial:** X via xAI API (out of credits), Bird-search vendored (missing npm dep)
- **Broken:** Reddit/TikTok/Instagram (need ScrapeCreators API key)
- **Path to X fix:** Install missing npm deps + log into X in Safari on this Mac
- **Slash command:** Not registered yet (`/last30days`)

## Pending Items
- [ ] Approve Windows node pairing when user runs `openclaw node run` on fx-programer-nb1
- [ ] Verify 13:00 vocab cron fires clean with owl-alpha + 300s timeout
- [ ] Wire up `/last30days` slash command
- [ ] Install bird-search npm deps for X search
